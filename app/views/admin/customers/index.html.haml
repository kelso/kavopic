%h2 Zákazníci

.table-responsive
  %table.table.table-bordered.table-sm
    %thead
      %tr
        %th Meno
        %th E-mail
        %th Registrácia
        %th Akcia
    %tbody
      - @customers.each do |customer|
        %tr
          %td= customer.full_name
          %td= customer.email
          %td= l customer.created_at, format: :short
          %td
            = link_to 'Zobraziť', [:admin, customer], class: 'btn btn-secondary btn-xs'
            = link_to 'Upraviť', [:edit, :admin, customer], class: 'btn btn-primary btn-xs'
